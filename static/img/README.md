# Image placeholders

Replace the empty placeholder files in `optimized/`, `optimized_webp/`, and their subfolders with your own project and article images. See `core/site_data.py` for the exact paths and filenames used by the site.
